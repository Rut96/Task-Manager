import "./Header.css";

export function Header(): JSX.Element {
    return (
        <div className="Header">

			<p>Search bar</p>
            <p>Workspace: <span>Current Workspace</span></p>

        </div>
    );
}
